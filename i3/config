set $mod Mod4

# Don't let focus follow the mouse
focus_follows_mouse no

# Make the currently focused window a scratchpad
bindsym $mod+Shift+e move scratchpad

# Show the first scratchpad window
bindsym $mod+e scratchpad show

new_window normal

for_window [class="Vlc"] floating enable
for_window [class="mpv"] floating enable
for_window [class="Gxmessage"] floating enable
for_window [class="Alarm-clock-applet"] floating enable

# font for window titles
font xft:sans 8

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# kill focused window
bindsym $mod+x kill

# change focus
bindsym $mod+n focus left
bindsym $mod+r focus down
bindsym $mod+g focus up
bindsym $mod+t focus right

# move focused window
bindsym $mod+Shift+n move left
bindsym $mod+Shift+r move down
bindsym $mod+Shift+g move up
bindsym $mod+Shift+t move right

# switch to next/prev workspace on output
bindsym $mod+Left workspace prev_on_output
bindsym $mod+Right workspace next_on_output
# split in horizontal orientation
bindsym $mod+m split h

# split in vertical orientation
bindsym $mod+b split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen

# change container layout (stacked, tabbed, default)
bindsym $mod+u layout stacking
bindsym $mod+i layout tabbed
bindsym $mod+a layout default

# toggle tiling / floating
bindsym $mod+Shift+l floating toggle

# change focus between tiling / floating windows
bindsym $mod+l focus mode_toggle

# focus the parent container
bindsym $mod+s focus parent

# switch to last used workspace
bindsym $mod+Escape workspace back_and_forth

# prompt for a new workspace to be created
bindsym $mod+Ctrl+n exec i3-input -F 'workspace %s' -P 'New workspace: '

# prompt for new workspace name
bindsym $mod+Ctrl+r exec i3-input -F 'rename workspace to %s' -P 'New name: '

# switch to workspace
bindsym $mod+1 workspace number 1
bindsym $mod+2 workspace number 2
bindsym $mod+3 workspace number 3
bindsym $mod+4 workspace number 4
bindsym $mod+5 workspace number 5
bindsym $mod+6 workspace number 6
bindsym $mod+7 workspace number 7
bindsym $mod+8 workspace number 8
bindsym $mod+9 workspace number 9
bindsym $mod+0 workspace number 10

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace number 1; workspace number 1
bindsym $mod+Shift+2 move container to workspace number 2; workspace number 2
bindsym $mod+Shift+3 move container to workspace number 3; workspace number 3
bindsym $mod+Shift+4 move container to workspace number 4; workspace number 4
bindsym $mod+Shift+5 move container to workspace number 5; workspace number 5
bindsym $mod+Shift+6 move container to workspace number 6; workspace number 6
bindsym $mod+Shift+7 move container to workspace number 7; workspace number 7
bindsym $mod+Shift+8 move container to workspace number 8; workspace number 8
bindsym $mod+Shift+9 move container to workspace number 9; workspace number 9
bindsym $mod+Shift+0 move container to workspace number 10; workspace number 10


# class                 border  backgr. text    indicator
client.focused          #00BCD4 #00BCD4 #282828 #cc241d 
client.focused_inactive #1d2021 #1d2021 #928374 #928374
client.unfocused        #32302f #32302f #928374 #928374
client.urgent           #cc241d #cc241d #ebdbb2 #cc241d

bar {
  position top
  status_command conky -c .conkyrc_i3
  colors {
    separator          #928374
    background         #282828
    statusline         #ebdbb2
    focused_workspace  #00BCD4 #00BCD4 #282828
    active_workspace   #1d2021 #1d2021 #928374
    inactive_workspace #32302f #32302f #928374
    urgent_workspace   #cc241d #cc241d #ebdbb2
  }
}

# start window manager target
exec systemctl --user import-environment
exec systemctl --user start window-manager.target
